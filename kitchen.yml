---
driver:
  name: terraform
  root_module_directory: "test/fixtures/modules/test"
provisioner:
  name: terraform
verifier:
  name: terraform
  systems:
    - name: default
      backend: aws

platforms:
  - name: aws

suites:
  - name: default
